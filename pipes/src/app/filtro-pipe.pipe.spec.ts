import { FiltroPipePipe } from './filtro-pipe.pipe';

describe('FiltroPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroPipePipe();
    expect(pipe).toBeTruthy();
  });
});
